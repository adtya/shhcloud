# Word Cloud generator for your shell's command history

#### Installation:

```sh
pip install shhcloud --user # doesn't work yet
# Clone the repo and run
pip install -e . --user
```

#### Usage

```sh
sshcloud
```
